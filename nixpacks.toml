# Railway/Nixpacks: deploy FastAPI from api/ (not desktop requirements.txt at repo root).

[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r api/requirements.txt"]

[start]
cmd = "python -m uvicorn api.app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
