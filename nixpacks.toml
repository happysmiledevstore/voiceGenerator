# Railway/Nixpacks: deploy FastAPI from api/ (not desktop requirements.txt at repo root).

providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["python -m pip install -r api/requirements.txt"]
